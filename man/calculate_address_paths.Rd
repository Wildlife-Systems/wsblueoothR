% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{calculate_address_paths}
\alias{calculate_address_paths}
\title{Track Address Paths Through Devices}
\usage{
calculate_address_paths(
  input_files,
  top_n = 10L,
  progress_interval = 10000L,
  device_filter = characterVector(),
  min_date = "",
  max_date = ""
)
}
\arguments{
\item{input_files}{Character vector of file paths to process.}

\item{top_n}{Integer. Number of top addresses to return (by detection count). Default is 10.}

\item{progress_interval}{Integer. How often to print progress (0 = no progress). Default is 10000.}

\item{device_filter}{Character vector. Filter by specific device IDs. Empty = all devices.}

\item{min_date}{String. Minimum date in YYYYMMDD format. Empty = no minimum.}

\item{max_date}{String. Maximum date in YYYYMMDD format. Empty = no maximum.}
}
\value{
A data.frame with columns:
\describe{
\item{address}{Bluetooth MAC address}
\item{detection_count}{Total number of detections}
\item{device_count}{Number of unique devices that detected this address}
\item{first_seen}{First detection timestamp (YYYYMMDD-HHMMSS)}
\item{last_seen}{Last detection timestamp (YYYYMMDD-HHMMSS)}
\item{path}{Chronological path through devices (e.g., "16 -> 18 -> 16 -> 21")}
}
}
\description{
For each address, creates a chronological path showing which devices detected
the address over time. Returns the top N addresses by detection count.
}
\examples{
\dontrun{
paths <- get_address_paths("data/combined_sort.txt", top_n = 20)
paths <- get_address_paths(c("file1.txt", "file2.txt"), top_n = 50)
}

}
