% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duration_visualisation.R
\name{plot_duration_heatmap}
\alias{plot_duration_heatmap}
\title{Plot Duration Heatmap by Device and Date}
\usage{
plot_duration_heatmap(duration_data, time_unit = "hours", stat = "mean")
}
\arguments{
\item{duration_data}{A data.frame from \code{get_address_duration()} or \code{get_average_address_duration()}.}

\item{time_unit}{Character. Unit for durations: "seconds", "minutes", or "hours". Default is "hours".}

\item{stat}{Character. Statistic to display: "mean", "median", "max", or "count". Default is "mean".
For median aggregation data, "count" shows the number of unique addresses per device-date.}
}
\value{
A base R heatmap showing duration patterns.
}
\description{
Creates a heatmap showing detection durations across devices and dates.
Works with both detailed data from \code{get_address_duration()} and
aggregated data from \code{get_average_address_duration()}.
}
\examples{
\dontrun{
# With detailed data
durations <- get_address_duration("data/combined_sort.txt")
plot_duration_heatmap(durations)
plot_duration_heatmap(durations, stat = "median")

# With median aggregation data
median_dur <- get_average_address_duration("data/combined_sort.txt")
plot_duration_heatmap(median_dur)
plot_duration_heatmap(median_dur, stat = "count")
}

}
